# Generated by Ansible for {{ ansible_hostname  }}.{{ network.domain }}

auto lo
  iface             lo inet loopback

auto enp3s0
  iface             {{ ansible_default_ipv4.interface }} inet static
  address           {{ network.dhcp }}
  netmask           {{ network.netmask }}
  gateway	    {{ network.gateway }}
  dns-domain        {{ network.domain }}
  dns-nameservers   {{ network.dns }}
